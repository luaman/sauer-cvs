{
    IBClasses = (
        {CLASS = ConsoleView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                helpAction = id; 
                multiplayerAction = id; 
                openUserdir = id; 
                playAction = id; 
                playMap = id; 
                playRpg = id; 
            }; 
            CLASS = Launcher; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                console = ConsoleView; 
                keys = NSArrayController; 
                maps = NSArrayController; 
                multiplayer = NSButton; 
                resolutions = NSPopUpButton; 
                view1 = NSView; 
                view2 = NSView; 
                view3 = NSView; 
                view4 = NSView; 
                view5 = NSView; 
                window = NSWindow; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}