{
    IBClasses = (
        {CLASS = ConsoleView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            CLASS = DisclosureController; 
            LANGUAGE = ObjC; 
            OUTLETS = {d1 = NSButton; d2 = NSButton; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {helpAction = id; multiplayerAction = id; playAction = id; }; 
            CLASS = Launcher; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                console = ConsoleView; 
                fullscreen = NSButton; 
                multiplayer = NSButton; 
                occlusion = NSButton; 
                resolutions = NSPopUpButton; 
                serverOptions = NSTextField; 
                shader = NSButton; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}